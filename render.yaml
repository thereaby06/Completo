services:
  # Backend Service
  - type: web
    name: workshop-backend
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm install && cd backend && npm install && npx prisma generate && node prisma/setup-db.js
    startCommand: cd backend && node src/index.js
    envVars:
      - key: NODE_VERSION
        value: 18.0.0
      - key: DATABASE_URL
        fromDatabase:
          name: completo-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true # Genera un secret seguro automáticamente
      - key: PORT
        value: 10000
      - key: RESET_DB
        value: "true"
      - key: RUN_SEED
        value: "true"

  # Frontend Service
  - type: static
    name: workshop-frontend
    runtime: static
    buildCommand: cd frontend && npm install && npm run build
    publishDir: frontend/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://workshop-backend.onrender.com # Cambiar por tu URL de Render real
